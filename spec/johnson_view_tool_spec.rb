require "spec_helper"

describe JohnsonViewTool do
  it "has a version number" do
    expect(JohnsonViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
